{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "拡張機能を実行 (TechWord)",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/packages/vscode"],
      "outFiles": ["${workspaceFolder}/packages/*/dist/**/*.js"],
      "preLaunchTask": "npm: build"
    }
  ]
}
